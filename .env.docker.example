# Copie este arquivo para ".env" na raiz do projeto e preencha os valores.
DB_PASSWORD=uma_senha_forte_aqui

# Gere uma chave secreta base64 de pelo menos 256 bits, por exemplo com:
# openssl rand -base64 32
JWT_SECRET=coloque_aqui_sua_chave_jwt_em_base64
